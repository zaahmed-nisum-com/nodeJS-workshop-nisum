const subscriptionModel = require("../models/subscription");
const userModel = require("../models/user");
const mongoose = require("mongoose");
module.exports = {
  roleAccessForMovie: async (req, res, next) => {
    try {
    } catch (error) {
      throw new Error(error);
    }
  },
};
